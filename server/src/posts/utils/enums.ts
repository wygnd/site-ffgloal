export enum Order {
  asc = "ASC",
  desc = "DESC",
}

export enum OrderBy {
  id = "post_id",
  title = "title",
  menu_order = "menu_order"
}

export enum PostStatus {
  all = "*",
  publish = "Publish",
  draft = "Draft",
  hidden = "Hidden"
}