import React from 'react';
import Navbar from "@/components/navbar/Navbar";

const Header = () => {
  return (
    <header>
      Header
      <hr/>
      <Navbar/>
    </header>
  );
};

export default Header;